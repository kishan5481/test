event deligator
diff b/w theread and process
jwt

object interview question



set is a unique collection data structure.

it has few methods and properties.

let set = new Set([1,2,3]);

set.add(4);
set.has(4);
set.size;
set.delete(3);
set.clear();




13.
18
19
21
24
27,
30-35: symbol
41



React:

useState() : hook to update the previous state with current state always use like this 

let [count,setCount] = useState(0);

setCount(previous => previous +1);

*) usestate hook doent merge objects automatically we need to merge it manually.